# Used by docker-compose.yml to configure the Postgres container.
# Copy this file to .env and adjust values as needed.

POSTGRES_USER=abm_user
POSTGRES_PASSWORD=abm_password
POSTGRES_DB=ai_business_manager
